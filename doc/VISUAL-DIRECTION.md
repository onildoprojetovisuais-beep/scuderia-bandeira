# VISUAL-DIRECTION.md — Nova Home Scuderia Bandeiras

**Status:** direção convergente e final desta fase de pesquisa/decisão. Este documento resolve os conflitos apontados pelo Agente I (Crítico) entre os relatórios A–H e K, e substitui qualquer paleta de cor, tratamento de Caterham ou número de orçamento de motion divergente citado nos relatórios individuais.
**Fonte única de texto:** `doc/copy-final-aprovada.md`. Nenhuma palavra da copy foi alterada, reordenada como decisão definitiva, ou reescrita neste documento.
**Prioridade quando há tensão (ordem do brief):** 1) identidade → 2) clareza → 3) narrativa → 4) experiência → 5) conversão → 6) motion → 7) tecnologia → 8) estética.
**Como ler isto:** cada seção dá UMA decisão. Onde os relatórios divergiam, a divergência foi resolvida aqui (com 1–2 frases de justificativa) ou — quando só o usuário pode decidir (copy, fato, direito de imagem, endpoint) — foi movida para a seção final "DECISÕES E APROVAÇÕES PENDENTES DO USUÁRIO".

---

## 1. CONCEITO CENTRAL

**"As mesmas peças, montadas de um jeito diferente a cada vez."**

A copy já diz isso literalmente: *"Pilotos, carros, pista, estrutura e produção são as mesmas peças. O que muda é o projeto que se monta com elas."* (`copy-final-aprovada.md:73-75`). O Agente D leu essa frase e propôs um sistema chamado internamente "SETUP": um kit fixo de cinco elementos que se recombina por formato (Team Building, Caterham, Bandeiras Empresarial, Conteúdo).

**Teste "troque o logo por outra marca" (aplicado pelo Crítico) — resultado: sobrevive parcialmente, revisado aqui.**
- O que **não** sobrevive ao teste: o dispositivo gráfico literal que D chamou de "Régua de Peças" (rótulos PILOTOS·CARROS·PISTA·ESTRUTURA·PRODUÇÃO, ligados/desligados, repetidos em 6 dobras) é um sistema de design intercambiável — qualquer agência aplicaria o mesmo dispositivo a qualquer B2B "de peças que se combinam". Ele também cria uma segunda taxonomia que o visitante tem que aprender por cima da navbar, que já lista os formatos reais. **Cortado como widget recorrente** (decisão no §2 e §12).
- O que **sobrevive**: o princípio de composição — mostrar o mesmo carro, as mesmas pessoas e o mesmo lugar aparecendo em contextos diferentes a cada dobra — é fiel à copy, não é clichê de "somos um ecossistema", e não é transferível para outra marca porque depende do material real da Scuderia (a torre vermelha real, o F3 na parede do lobby, os 4 pilotos, o Caterham na Fazenda Capuava). É esse material — não o sistema gráfico — que assina a página.

**Decisão final:** o conceito central é a **recombinação de cinco elementos reais (pilotos, carros, pista, estrutura, produção) através de fotografia e vídeo verdadeiros**, guiando a escolha de imagem e a composição de cada dobra. Não existe mais um widget de UI chamado "Régua de Peças" repetido pela página; existe, no máximo, uma única aparição editorial na dobra 03 (Ecossistema), onde a própria copy apresenta a ideia (ver §15.05).

---

## 2. DIREÇÃO DE ARTE

Base: relatório D (Agente Art Director), com três correções obrigatórias do Crítico aplicadas:

1. **Cor de marca corrigida** para a cor real medida por amostragem de pixel no logo (#FF4747), não a cor do arquivo de paleta desatualizado (#F80100) que D e G usaram (§9).
2. **Dobra Caterham redesenhada** do zero com material real (POV de passageiro, vídeo 1080p, 4K por modelo, foto 5000×3333) em vez do tratamento 100% tipográfico que D propôs por desconhecer esse material (§15.05).
3. **Régua de Peças reduzida** a uma única aparição editorial (§03), não mais um sistema de rótulos repetido em 6 dobras (§1, §12, §15).

O restante da direção de D permanece válido e é a base desta direção: recusa de renders 3D de patrocínio Stock Car e de qualquer imagem que sugira "equipe em campeonato" (grid de largada, placar, contador de credenciais); uso do material fotográfico real do complexo como protagonista das dobras de espaço; paleta clara ("papel" quente, derivado do tijolo/concreto/âmbar das fotos reais) alternando com o escuro de marca; tipografia Special Gothic Condensed One + Inter; corte-r como único recorte de forma do sistema (nunca em botão, card ou input — só em fotografia, no máximo uma por viewport); uma silhueta diferente por dobra (proibido o padrão headline+parágrafo+4 cards+CTA repetido).

**O que este documento adiciona à direção de D:** a assinatura visual da página passa a ser, em ordem de importância, (1) o material real (tijolo, torre, F3 na parede, Caterham, os 4 pilotos), (2) a paleta corrigida, (3) a tipografia, e só depois (4) dispositivos gráficos de sistema (corte-r, filete, numerais tabulares) — nunca o inverso.

---

## 3. PRINCÍPIOS EXTRAÍDOS DOS BENCHMARKS (do Agente C, filtrados)

Os 13 princípios do relatório C são mantidos; os seguintes são os que efetivamente orientam decisões de dobra nesta direção (o texto completo de cada um está em C§3, aqui só a aplicação final):

1. **Concentre a produção máxima em um momento** (P1 de C) — o Hero e a Caterham recebem a maior densidade de imagem/vídeo; Sobre e Faixa de Autoridade caem para tipografia sobre fundo sólido.
2. **Mude o registro inteiro ao trocar de assunto** (P2) — Bandeiras Empresarial é a dobra que muda de temperatura: sai do preto/carro e entra no papel quente/arquitetura/luz natural.
3. **Uma gramática de motion, várias silhuetas** (P3) — os tokens de motion (§13) são únicos; a forma de cada dobra muda, a física do movimento não.
4. **Prova = case aplicado primeiro, logos depois, com hierarquia** (P4) — a dobra 10 abre com o case PETRONAS (espaço + pessoa + ação), a lista de marcas vem por último e nunca como "parede de logos" sem hierarquia.
5. **Números como ficha de engenharia, não troféu** (P5) — todo número da copy (84 cv/490 kg, 300 pessoas, 320 largadas) é tratado como dado técnico tabular, nunca como contador de placar.
6. **O espaço como percurso com escala humana** (P6) — a "visita" da dobra 06 é uma sequência de lugares com metragem, não uma galeria de "salão de festas".
7. **O carro entra pelo ponto de vista do humano** (P7) — decisivo para a Caterham redesenhada (§15.05): POV do passageiro, nunca carro recortado flutuando.
8. **Venda pelo objetivo do comprador, não por cardápio** (P8) — a dobra 04 abre pelo objetivo ("cliente que você quer aproximar, time que você quer unir...") antes de listar formatos.
9. **Ritmo pico/vale explícito** (P9) — ver curva de ritmo completa em §15 (introdução) e na tabela de intensidade por dobra.
10. **Autoridade de pessoa = retrato forte + um fato verificável** (P10) — a Faixa de Autoridade é linha de créditos, não card de equipe.
11. **Conteúdo como bastidor/frame, sem tom de agência** (P11) — dobra 07 usa linguagem de contact sheet, nunca "reel" com timecode/REC.
12. **Canal humano visível + CTA com pesos diferentes** (P12) — WhatsApp sempre presente como porta de baixo compromisso; "Crie um projeto" como CTA primário único por viewport.
13. **Bookend** (P13) — o CTA final e o footer retomam a composição calma do Hero (mesma família de imagem, sem carro, sem ruído).

**Anti-padrões do setor banidos** (lista completa em C§4): cookie banner cobrindo o hero, widget de campeonato (standings/calendário/countdown), hero em carrossel, merch/membership/gamificação, bloco de cor de marca chapado, condensada caixa-alta em tudo, bandeira quadriculada como motivo decorativo, logo salad sem hierarquia, superlativo vazio ("adrenalina", "inesquecível"), prova inflada, cardápio em cards iguais, negócio escondido atrás do esporte, foto de "helmet high-five", heritage/hall-of-fame como muleta (a Scuderia não disputa categoria oficial hoje — §14/§15 tratam isso).

---

## 4. STACK ENCONTRADA (do Agente B, resumida)

- **HTML/CSS/JS estático, sem framework, sem bundler.** Consenso real e bem fundamentado entre B e G (o Crítico confirma: "executável tecnicamente: sim"). Nenhum requisito do projeto (uma página, sem SSR, sem roteador) justifica migrar de stack.
- **GSAP, Lenis e Locomotive não estão instalados em nenhum projeto real do workspace** — só intenção em READMEs. Lenis é desaconselhado (conflita com precisão/engenharia da marca, com `sticky`, com acessibilidade). GSAP fica reservado como escalonamento condicional, não decisão default (ver §13).
- **Ferramentas de mídia confirmadas e testadas:** Pillow 12.2 (AVIF/WebP funcionando, reduções de 88–99% medidas por G), ffmpeg 8.1.1 com libx264/libsvtav1/libvpx-vp9, fontTools+brotli para subsetting de fonte, Playwright 1.61 para QA multi-viewport.
- **`ui-ux-pro-max`:** só o `SKILL.md` funciona (scripts/dados são symlinks quebrados) — usar como checklist de revisão, nunca `--design-system`/`search.py`.
- **Referências da Creative Stack (`00-STACK/01-REFERENCES`)** alimentaram o benchmark do Agente C; nenhum template ou componente pronto existe em `02-DESIGN-SYSTEM`/`03-COMPONENTS`/`04-MOTION` (só READMEs vazios) — este projeto é o primeiro a produzir esses padrões.
- **QA:** `agent-browser` 0.27.0 (CLI + Chrome via CDP) e Playwright (Chromium/Firefox/WebKit) são as duas ferramentas disponíveis e testadas para screenshot/vitals/reduced-motion por viewport.

---

## 5. SKILLS ESCOLHIDAS

Consolidado do relatório B, decisão final para a fase de construção:

| Skill/ferramenta | Uso |
|---|---|
| `frontend-design` | Disciplina de direção de arte, com filtro: ignorar "custom cursor, grain, gradient mesh, maximalist chaos" (proibidos pelo brief). |
| `ui-ux-pro-max` | Só as tabelas de checklist (contraste, alvo de toque, `viewport-units`, `excessive-motion`, `motion-consistency`) como revisão pré-entrega. Não rodar scripts quebrados. |
| `gsap-scrolltrigger` | Só a referência de API, condicional a GSAP ser efetivamente adotado (não é o default — ver §13). Não acionar o agente `choreographer` (não acrescenta nada ao SKILL.md). |
| `agent-browser` + Playwright | QA visual e de performance nos 8 viewports (320/375/390/430/768/1024/1440/1920), com `reduced-motion` e sem JS. |
| `direction`, `creative-brief`, `ref` | **Não usar** nesta fase — o conceito já está definido e essas skills reabririam decisões estratégicas fora de escopo. |
| `stop-slop` | Não usar na copy (fonte de verdade fixa). |
| `threejs-webgl`, `web3d-integration-patterns` | **Não usar** — nenhuma dobra exige 3D/WebGL; o brief proíbe efeito gratuito e o custo (600+ KB medido em teste-piloto) não se justifica. |
| Skills de deploy (`vercel:*`) | Não acionar nesta fase (proibido fazer deploy). |
| Skills de copy (`copywriting`, `landing-page-copywriter`, etc.) | Não usar — copy é fonte fixa. |

---

## 6. ASSETS EXISTENTES (resumo do Agente K + correção da Caterham)

### 6.1 O que existe e é forte, dentro do projeto NOVA
- **Fotografia do complexo** (`Imagens/complexo/1–7.png`, `local/auditorio.jpg`, `local/gastronomia.jpg`, `o que é/*.png`, `final/imagem final@2x.png`): fotografia profissional de arquitetura, luz real, é o principal ativo para as dobras 02, 03, 06 e 11.
- **Retratos editoriais** (`Imagens/pilotos-premium/{atila-abreu,cristian-fitipalidi,ingo-hoffman,nelson-piquet}.jpg`): série coesa, tratamento consistente, usável na Faixa de Autoridade e em 08/09 (resolução moderada, ~900×1192 — suficiente em coluna, não para full-bleed).
- **Truck Lounge / Festival de Interlagos** (`experiencia do evento/*.png`): fotografia real de evento, para a dobra 07.

### 6.2 Correção crítica: Caterham NÃO precisa ser tipográfica
O relatório D e o relatório F desenharam a dobra 05 (Caterham) inteiramente tipográfica por não encontrarem nenhuma foto/vídeo de Caterham **dentro** do projeto NOVA. O Crítico (I) verificou pessoalmente — abrindo e medindo os arquivos com `ffprobe`/Pillow — que existe material profissional real em `C:\Projetos ClaudeCode\SCUDERIA-BANDEIRA\Projeto-caterham\Caterham - Festival - Interlagos\`:

- `Brand/Imagem/seção-5/drive-experience.jpg` (2400×1600): **exatamente** o enquadramento POV do passageiro que a copy descreve — capacete do piloto profissional em primeiro plano, mãos no volante, pista à frente.
- `assets/videos/desk.mp4` (1920×1080, H.264, 40 s) e `mobile.mp4` (1080×1920, 40 s): helicóptero sobrevoando, pelotão de Caterham em curva, trecho de POV de cockpit.
- `video/modelos-caterham/SS600.MOV` e `420R.MOV` (4K HEVC, 19–25 s cada; versões web `ss600.mp4`/`420r.mp4`, 1280×720, ~4,6 MB): órbita de câmera em cada modelo — material para a ficha "84 cv/490 kg" e "210 cv/535 kg".
- `Brand/Imagem/seção-2/uma-experiencia-criada-para-ser-vivida.jpg` (5000×3333): Caterham solitário em reta cinematográfica — candidato a still/poster de abertura.
- `assets/images/cars/gallery-01..05` (lateral, frente, topo, escapamento, roda): detalhes de engenharia para compor a ficha numérica.
- `nova-foto-time.jpg`/`seção-2/time.jpg`: 6 pilotos-instrutores em macacão verde sob o pórtico "RACE EXPERIENCE" — nenhum identificado como Rubens ou Rafael Suzuki pelo Crítico, mas **precisa de confirmação humana** antes de publicar (pendência §16).

**Este material é a base do redesenho da dobra 05** (§15.05). O uso desse material está condicionado a confirmação de direitos de imagem (o material está num projeto irmão, com fotógrafo credenciado "dudabairros" e participantes reconhecíveis) — tratado como pendência formal em §16, não decidido aqui.

### 6.3 O que NÃO pode ser usado (confirmado por K, D e I)
- **Rubens Barrichello e Rafael Suzuki**: aparecem em 11+ arquivos do projeto NOVA (avatares, retratos premium, cutouts, hero antigo) — nenhum entra na página nova. **Atenção especial:** `Imagens/nosso time/imagem time bandeiras desk.jpg` (o mosaico "SOMOS TODOS SCUDERIA BANDEIRAS", única prova visual de "mais de 50 profissionais") pode conter os dois por comparação visual de K — **não confirmado**, exige checagem humana antes de qualquer publicação (pendência §16, item 6).
- **Renders 3D de patrocínio Stock Car** (`carros/*`, `assets/car/*`) e a imagem `complexo/bg.jpg` (CGI de carro em arquibancada lotada): era de campanha Stock Car, contradizem o fato de que a Scuderia não disputa categoria oficial hoje.
- **`hero-conexoes-v2.png`/`-mobile-v2.png`** (não commitados): túnel escuro com riscos de luz vermelha, aparência de imagem gerada por IA — é exatamente "preto+vermelho+speed lines genéricas", proibido pelo brief. **Não usar como hero.**
- **`Imagens/marcas parceiras/MARCAS PARCEIRAS.png`**: 30 logos, lista diverge da copy aprovada (15 marcas) — não publicar como está.
- **`Imagens/lei de incentivo/*`, `Imagens/stock car em numeros/*`**: fora da copy, um deles contém CNPJ/dados bancários de terceiro — nunca usar.

### 6.4 Pipeline de mídia (do Agente G, confirmado por medição real)
AVIF (primário, q≈52) + JPEG/WebP fallback; larguras 480/800/1280/1920; nomes com hash de conteúdo → cache imutável. Reduções medidas: `complexo/6.png` de 5,6 MB → 245 KB (AVIF 1920w) / 130 KB (1280w); `local/auditorio.jpg` de 13,3 MB → 145 KB (1920w). Vídeo: H.264 universal + AV1 opcional, `preload="none"`, poster como candidato a LCP, sem áudio nos loops de fundo.

---

## 7. SISTEMA DE GRID

- **Container:** máximo 1440px, margem lateral fluida `clamp(16px, 4vw, 64px)`, gutter `clamp(12px, 1.6vw, 24px)`.
- **Colunas:** 12 (≥1024px) · 8 (640–1023px) · 4 (<640px). `subgrid` nos componentes que precisam alinhar cartões internos.
- **Container queries** nos componentes reutilizáveis (scroller, cartão de piloto, ficha de dado): `container-type: inline-size`. Breakpoints de estrutura de página em 3 pontos literais, mobile-first (`min-width`): **640px, 1024px, 1280px** (o breakpoint de 1280px da navbar — §13 — é o mesmo ponto de virada estrutural do grid de 12 colunas).
- **Quebras intencionais de grid** (no máximo uma por dobra, para não virar ruído): (1) foto sangrando até a borda da viewport em um dos lados; (2) título/numeral atravessando colunas e "pendurado" na margem esquerda; (3) tiras com deslocamento vertical (dobra 03).
- **Unidades de altura:** `svh` para hero/runway/cenas de tela cheia (estável quando a barra de endereço do mobile some/volta); `dvh` para modal/menu/overlays (acompanha as barras dinâmicas). Nunca `100vh` puro nem `100vw`.

---

## 8. TIPOGRAFIA

**Confirmado:** Special Gothic Condensed One existe no Google Fonts (peso único 400, verificado via API pelo Crítico) e é a fonte de marca (`Brand/tipografia.png`). Segunda fonte confirmada por convergência de todos os relatórios (D, G, H): **Inter** (variável, 300–700, uso real 400/500/600).

- **Display: Special Gothic Condensed One**, único peso 400 — `font-synthesis: none` (nunca negrito falso). Cobre acentuação PT-BR completa. Fallback de métrica: `Barlow Condensed` (já usada no site atual) com `size-adjust` calibrado para CLS zero na troca.
- **Texto e rótulos: Inter variável.** `font-feature-settings: "tnum","case"` em dados/rótulos.
- Sem terceira família, sem serifa (evita a fórmula "luxury automotive editorial" que o brief não pede).

**Escala fluida (piso 320px → teto 1440px):**
```
--t-mega:  clamp(3.5rem, 12.8vw, 11.5rem)   /* H1 Hero */
--t-num:   clamp(5rem, 22vw, 18.75rem)      /* numerais de ficha (84/490/300) */
--t-xxl:   clamp(2.75rem, 1.3rem + 6.4vw, 7.5rem)
--t-xl:    clamp(2.25rem, 1.4rem + 4vw, 5rem)
--t-l:     clamp(1.75rem, 1.3rem + 2vw, 3rem)
--t-m:     clamp(1.25rem, 1.1rem + .7vw, 1.75rem)   /* lead */
--t-body:  clamp(1rem, .95rem + .25vw, 1.1875rem)   /* corpo, nunca <16px */
--t-small: .875rem
--t-label: .75rem                                    /* mínimo 12px */
```
Regras: caixa-alta só em rótulos/eyebrows curtos (nunca em headline de dobra calma); entrelinha 0.86–0.92 em display de impacto, 1.0 em display de dobra calma; corpo em Inter 400, entrelinha 1.55–1.6, medida 56–64ch (34–40 caracteres/linha em mobile). Números-chave da copy (84 cv, 490 kg, 300 pessoas, 320 largadas) em Special Gothic + `tabular-nums`, unidade em Inter menor ao lado — nunca contador tipo placar fora da dobra Caterham (ver §13).

---

## 9. CORES

**Esta seção substitui integralmente as paletas dos relatórios D e G.** Ambos usaram `#F80100` como "vermelho de marca" porque essa é a cor gravada no arquivo `Brand/paleta de cor.png`. O Crítico amostrou pixels do arquivo de logo real (`Brand/1124_Bandeiras_RGB_L-RW.png`, 66.504 amostras de canal alfa >200) e confirmou que a cor que efetivamente aparece na tela é **`#FF4747`** — o arquivo de paleta está desatualizado em relação ao logo entregue. Esta direção usa a cor real do logo como base do sistema.

### 9.1 Tokens
```
/* SUPERFÍCIES ESCURAS (marca) */
--asfalto:      #0F0F0F   /* fundo escuro padrão — sem mudança, bate com a paleta e com o logo */
--asfalto-2:    #1A1A1A   /* superfície elevada / faixa de bastidor */
--branco:       #FFFFFF   /* marca, sem mudança */

/* SUPERFÍCIE CLARA (extensão de design, não é 4ª cor "oficial" do brand guide —
   derivada da fotografia real do complexo: tijolo, concreto, âmbar) */
--papel:        #F1EEE8
--papel-2:      #E8E3DA

/* VERMELHO — base real do logo, com um único derivado calculado para os casos que reprovam AA */
--vermelho:       #FF4747   /* cor real do logo (medida por amostragem de pixel). Acentos, ícones, filete,
                                texto/ícone sobre fundo ESCURO, preenchimento de botão com texto ESCURO por cima */
--vermelho-profundo: #C81E1E /* derivado do mesmo matiz, mais escuro — SÓ para os dois casos em que
                                #FF4747 reprova AA (ver 9.2): texto vermelho sobre papel claro, e
                                preenchimento de botão que precise de texto branco em cima */
```

### 9.2 Contraste — a conta (WCAG, luminância relativa sRGB)
| Combinação | Razão calculada | Veredito |
|---|---|---|
| `--vermelho` (#FF4747) texto sobre `--asfalto` (#0F0F0F) | **5,70:1** | **Passa AA** (texto normal, ≥4,5:1) |
| `--asfalto` (#0F0F0F) texto sobre `--vermelho` (#FF4747) — ex.: botão com fundo vermelho e rótulo em tinta escura | **5,70:1** | **Passa AA.** Esta é a solução recomendada para CTAs: preenchimento no vermelho real da marca, rótulo em tinta escura (não branca) |
| `--branco` (#FFFFFF) texto/rótulo sobre `--vermelho` (#FF4747) | **3,36:1** | **REPROVA AA** para texto normal (passa só para texto grande ≥24px ou componente de UI ≥3:1). Nunca usar botão vermelho + texto branco pequeno com `--vermelho`. Se o preenchimento precisar de texto branco, usar `--vermelho-profundo` (ver abaixo) |
| `--branco` sobre `--vermelho-profundo` (#C81E1E) | **5,74:1** | Passa AA — usar aqui quando o botão precisar de texto branco |
| `--vermelho` (#FF4747) texto sobre `--papel` (#F1EEE8, superfície clara) | **2,90:1** | **REPROVA AA** mesmo para texto grande. Nunca usar a cor real do logo como texto/ícone sobre a superfície clara |
| `--vermelho-profundo` (#C81E1E) texto sobre `--papel`/branco | **5,74:1** | Passa AA — usar este token sempre que precisar de vermelho como texto/ícone sobre fundo claro |

### 9.3 Regra de uso (para não decidir uma reprovação de AA sozinho)
- **Sobre fundo escuro (`--asfalto`):** o vermelho real da marca (`--vermelho` #FF4747) pode ser usado tanto como texto/ícone quanto como preenchimento de botão — neste último caso, o rótulo do botão vai em tinta escura (`--asfalto`), não em branco. Este é o padrão recomendado por toda a página, porque mantém a cor real do logo visível e passa AA (5,70:1).
- **Sobre fundo claro (`--papel`):** a cor real do logo reprova AA como texto/ícone (2,90:1). A solução aplicada aqui é o token derivado `--vermelho-profundo`, mesmo matiz, mais escuro, calculado para passar AA (5,74:1).
- **Pendência formal para o usuário:** se a marca exigir que **apenas** o vermelho puro `#FF4747` apareça em qualquer contexto de texto/ícone sobre fundo claro — sem o derivado mais escuro — essa combinação reprova AA e não tem correção sem mudar a cor ou o fundo. Este documento **não decide essa reprovação sozinho**: está registrado como decisão pendente em §16 (item 8). Até lá, a regra acima (com `--vermelho-profundo` sobre papel) é a aplicada.
- **Texto secundário:** `--tinta-2` (~72–78% de opacidade da tinta sobre papel, calculado para ≥4,5:1) e `--luz-2` (~72–78% de branco sobre asfalto, ≥4,5:1) substituem os tokens de opacidade baixa do site atual (`.16`/`.25`/`.32` de alfa), que reprovavam AA (medido em A: 1,5–2,9:1).
- **Nenhum fundo vermelho chapado como decoração de seção** — o vermelho aparece em: botão de ação, filete, ícone, um único bloco cheio no fecho da dobra Caterham (§15.05), e onde já existe naturalmente nas fotos (carro, poltronas, torre real do complexo).

---

## 10. SPACING

Escala base 8, com passos fluidos por `clamp()` para não pular bruscamente entre breakpoints:
```
--space-3xs: 4px   --space-2xs: 8px   --space-xs: 12px   --space-s: clamp(16px, 4vw, 24px)
--space-m:   clamp(24px, 5vw, 32px)   --space-l: clamp(32px, 6vw, 48px)
--space-xl:  clamp(48px, 8vw, 96px)   --space-2xl: clamp(80px, 12vw, 128px)   --space-3xl: clamp(96px, 15vw, 192px)
```
- **Padding vertical de dobra:** `--space-3xl` (clamp 96–192px) em dobras de respiro (Sobre, Palestras, CTA final); `--space-2xl` (80–128px) em dobras densas (Conteúdo, Pilotos & Time).
- **Mobile (<768px):** reduzir a distância mínima de reveal/stagger e o gutter lateral para `clamp(16px, 5.4vw, 48px)` (medido por H: 17px a 320px, 21px a 390px, 41px a 768px).
- **Raios:** 0 em tudo (fotos, cards, botões, inputs) — a única forma "não retangular" do sistema é o corte-r (fotografia, no máximo um por viewport). Sem sombra em conteúdo; o modal usa só scrim sólido.

---

## 11. LINGUAGEM FOTOGRÁFICA

Princípio: **a foto é evidência, não ambiente.** Lugar real, gente real, oficina real, Caterham real — nunca carro em borrão de velocidade, nunca composição sintética, nunca render 3D como imagem principal.

- **Tratamento único** (aplicado no build de imagem, não como filtro CSS em runtime): curva leve (+5% contraste), saturação -4%, temperatura levemente quente — realça o tijolo e o âmbar já presentes nas fotos reais do complexo, sem duotone nem viragem de cor artificial.
- **Retratos de piloto:** manter o tratamento P&B/colagem já existente em `pilotos-premium/`, escurecer o fundo 15–25% e recortar apertado; nunca acima de ~560px CSS de largura (a resolução original não sustenta full-bleed).
- **Fotos de evento/produção (contact sheet, dobra 07):** crop apertado + grão fino (opacidade ~0,05 no escuro) — esconde a diferença de qualidade entre fontes distintas e aproxima o conjunto visualmente.
- **Enquadramento:** vertical/simétrico é a assinatura (o material forte do complexo e dos pilotos é vertical); evitar o clichê horizontal "carro varrendo o quadro".
- **Vídeo (novo, com o material Caterham confirmado no §6.2):** hero e Caterham podem usar vídeo real; loop mudo, 6–12s, `preload="none"`, poster obrigatório, pausa fora da viewport, botão de pausa acessível (WCAG 2.2.2). Nunca autoplay com som. Nunca escrubado por scroll (a copy diz "o piloto conduz, você sente" — o visitante é passageiro, não deve controlar a velocidade do vídeo pelo scroll).
- **Sem:** speed lines, motion blur, glow/neon, bandeira quadriculada como motivo decorativo (só dentro de foto real, nunca como padrão/divisor), carbono fake, cursor customizado, partículas, HUD/velocímetro.

---

## 12. SISTEMA DE MOTION (do Agente F, com as correções da crítica)

### 12.1 Decisão tecnológica (resolvendo o conflito B/F/G)
**CSS puro + IntersectionObserver + CSS scroll-driven animations (`animation-timeline: view()`) sob `@supports`, como aprimoramento progressivo. Sem GSAP, sem Lenis, na v1.** Dois dos três relatórios com números reais de peso/performance (F e G) concordam e mediram; B apenas lista GSAP como ferramenta disponível, sem recomendar seu uso agora. Critério objetivo de escalonamento (não é decisão de gosto): reabrir GSAP **somente se**, em protótipo real, o pin/scrub da dobra Caterham ou Bandeiras Empresarial falhar em Safari 26 ou Chrome Android de gama média (jank >2 frames) **e** o fallback estático for rejeitado pelo cliente como empobrecedor.

### 12.2 Tokens (únicos, contrato entre design e implementação)
```
--dur-fast:   160ms   /* hover, focus, press, toggle */
--dur-medium: 360ms   /* reveal de texto/bloco, menu, modal */
--dur-slow:   720ms   /* mask/clip de mídia grande, hero */
--dur-settle: 1100ms  /* exceção: só mídia assentando e o contador do Caterham */
--ease-brake:  cubic-bezier(.22,1,.36,1)   /* entradas — frenagem tardia, sem overshoot */
--ease-drive:  cubic-bezier(.6,0,.2,1)     /* deslocamentos A→B com origem/destino visíveis */
--ease-trail:  cubic-bezier(.4,0,.7,.2)    /* saídas — solta o freio, sai em velocidade */
--ease-linear: linear                      /* só quando o scroll É a função de tempo (caminhada, linha do tempo) */
```
Proibido: `back.*`, `elastic.*`, `bounce.*`, spring exagerado, qualquer easing com overshoot (y>1) — o brief veta explicitamente.

### 12.3 Orçamento único de pin/scroll-driven (resolve o conflito F × H)
F autorizava até ~6 viewports de pin no desktop somando Caterham + Bandeiras Empresarial; H definia "um único pin em todo o site, só no mobile, só na Caterham" sem referenciar o teto de F. **Decisão final, dono único deste orçamento a partir de agora:**
- **Desktop:** no máximo **2 dobras pinadas na página inteira** — Caterham (~2,2–2,6 viewports) e Bandeiras Empresarial (~3–3,5 viewports, `--k:3`). Nenhuma outra dobra usa `position:sticky` como pin de narrativa (sticky simples de rótulo/legenda não conta neste teto).
- **Mobile:** **apenas 1 dobra pinada** — a Caterham, como runway `position:sticky` de ~220svh com 3 beats por sentinelas IO. A Bandeiras Empresarial no mobile **não** usa pin/hijack: vira caminhada vertical por "cômodos" (blocos de ~70svh empilhados, sem scroll-jacking).
- Todo pin exige mudança visível a cada ≤60svh de rolagem (nenhum scroll morto); reduced-motion sempre remove o pin e empilha o conteúdo estático e completo.

### 12.4 Vocabulário de movimento (resumo; ver F§4 para a tabela completa)
Reveal (opacity+translateY, 1×, IO), mask reveal de texto (por palavra, só em headlines de impacto — Hero, Caterham, headline protegida da 06), clip reveal de mídia (máx. 1 por viewport), sticky sem JS, parallax (1 camada, ±5% desktop/±2,5% mobile, só em imagem com overscan), scale de assentar (1.06→1), hairline draw. **Contadores: banidos em credenciais e métricas do complexo** (viram placar — proibido); **permitidos apenas nos 4 números de engenharia da Caterham** (84/490/210/535), 1× e com valor final sempre presente no HTML.

### 12.5 `prefers-reduced-motion` (obrigatório, política única)
Reduced-motion = sem deslocamento, escala, parallax, pin, scrubbing, vídeo autoplay, loop, contador. Preserva: cor/estado instantâneo, hierarquia, ordem, 100% do conteúdo. Estado-base do CSS é sempre o estado final (visível) — motion é camada de cima, nunca requisito para ler a página. Failsafe: se o JS falhar, um `setTimeout` de 4s força todos os elementos a `is-in` (nada fica invisível para sempre).

### 12.6 Banidos (herdados do site atual e do brief, lista consolidada)
Cursor customizado com rastro, partículas/canvas ambiente, marquee/carrossel infinito, auto-advance sem controle do usuário, `scrub` elástico em texto, efeitos por letra (typewriter/glitch), bounce/spring/tilt 3D/botão magnético, loader/splash, `filter:blur`/`backdrop-filter` animado, skeleton/shimmer para conteúdo pendente (o estado vazio deve ser limpo, não "carregando para sempre"), `scroll-behavior:smooth` sem gate de reduced-motion.

---

## 13. UX (do Agente E, com as correções da crítica)

### 13.1 Navbar — breakpoint resolvido
**1280px** é o breakpoint de colapso da navbar (não 1024px). E chegou a esse número por cálculo real de largura de caracteres dos 8 itens + CTA (~1155px necessários, medido); G havia proposto 1024px/`64em` por convenção de projeto, sem medição. Acima de 1280px: navbar em linha única, 3 grupos visuais separados por espaço/hairline (Scuderia·Ecossistema | Experiências·Caterham·Bandeiras Empresarial·Conteúdo | Pilotos & Time·Palestras) + CTA "Fale com o time". Abaixo de 1280px: barra compacta + menu em `<dialog>` de tela cheia.

### 13.2 Ordem das dobras — não reordenar
O relatório E propôs (P-E3) mover a Faixa de Autoridade para logo após o Hero, antes da §02 Sobre, citando risco de abandono. **Decisão: manter a ordem literal da copy aprovada** (Hero → Sobre → Faixa de Autoridade → Ecossistema...). O brief autoriza apenas "tratamento visual" das divergências de copy, não reordenar seções como decisão de design — isso seria uma decisão editorial/estrutural, não uma decisão de composição. A proposta de E fica registrada como **pedido de aprovação ao usuário** (§16, item 4), não como algo já aplicado. Mitigação sem reordenar: a §02 Sobre usa tipografia em níveis (lead grande + corpo) para reduzir a sensação de "muro de texto" antes da Faixa.

### 13.3 CTAs — destino de rótulos sem página própria
| CTA/rótulo | Destino aplicado nesta direção |
|---|---|
| "Fale com o time" (navbar) | WhatsApp direto (`+55 15 99857-0854`) — é o que a copy já indica ao lado do rótulo na navbar |
| "Ver o Bandeiras Empresarial" (§06) | Abre o modal com "Bandeiras Empresarial" pré-selecionado — destino mínimo e reversível; se o usuário quiser uma página/visita dedicada no futuro, isso substitui este default sem mudar o resto da dobra |
| "O time completo →" (Faixa de Autoridade) | Âncora de rolagem para a dobra 08 (Pilotos & Time) |
| "Crie um projeto com a Scuderia" (todas as ~6 ocorrências) | Abre o modal, com o campo "O que você gostaria de criar" pré-selecionado conforme a origem do clique (mapa completo em §15, cada dobra) |
| "Prefere conversar antes? Chame no WhatsApp" (§11) | WhatsApp direto, com texto contextual "cta final" |

### 13.4 Formulário — campo "O que você gostaria de criar": select vs. chips resolvido
E recomendava radios em chips (mostra o cardápio, 1 toque); G e H recomendavam `<select>` nativo no mobile. **Não é uma contradição de fato, é responsividade não unificada — resolvida aqui como um único componente:** **`<select>` nativo no mobile** (H mediu o risco real de estouro de altura do sheet com 8 chips + teclado) e **radios em chips no desktop** (onde a altura não é problema e o benefício de visibilidade de E se aplica). O valor pré-selecionado pela origem do CTA é idêntico nos dois modos.

### 13.5 Formulário — desligado até existir endpoint real
Confirmado pelo Crítico via grep: `openModal()` nunca é chamada hoje, e o endpoint é um placeholder (`SEU_CRM_ENDPOINT`) que sempre mostra sucesso mesmo em falha. **Decisão inegociável: nenhum CTA "Crie um projeto" abre um formulário morto.** Enquanto não existir endpoint aprovado (§16, item 11), todo CTA de conversão abre WhatsApp direto com texto contextual — exatamente o comportamento que já está em produção hoje. O formulário (com os campos definidos por E: Nome, Empresa, Telefone/WhatsApp, E-mail, interesse, quando/quantas, como prefere seguir, detalhar — sem CNPJ) só é ativado no dia em que o endpoint responder de verdade, e a tela de sucesso só aparece com confirmação 2xx do servidor.

### 13.6 Estados vazios (placeholders da copy)
Nenhum texto entre colchetes, "pendente", "a inserir" ou "a definir" chega ao visitante. Cada pendência (vídeo da convenção PETRONAS, capacidade por formato, depoimento nomeado, lista final de logos, prazo de resposta, e-mail/CNPJ) tem um componente que **não renderiza nada** quando o dado não existe e a dobra continua com início, meio e fim visuais sem ele — nunca skeleton, nunca caixa cinza vazia.

### 13.7 Acessibilidade (checklist herdado de E, aplicado)
`<dialog>` nativo para modal e menu (foco preso, `inert`, ESC, retorno de foco); `fieldset/legend` nos grupos de radio; contraste recalculado com a paleta corrigida (§9); alvos ≥44×44px; nenhum conteúdo essencial só em hover/toque (o site atual falha nisso: `.pc`, `.ic`, `.ec` só revelam por hover — corrigido); `autocomplete` correto em todos os campos; nunca "Recebemos" sem confirmação real do servidor (regra mais importante herdada do bug do site atual, protegida também pelo Crítico como inegociável).

---

## 14. RESPONSIVE (do Agente H, com as correções da crítica)

- **Mobile é a versão principal, não a reduzida.** Cada dobra tem um gesto de toque próprio (mapa completo em §15): hero estático-cinema → leitura em níveis (§02) → lista de créditos (Faixa) → mosaico de 5 peças (§03, único uso do princípio de recombinação, sem o widget de régua) → foco de leitura + trilho snap (§04) → **runway sticky de 220svh, única dobra pinada no mobile** (§05 Caterham) → cenas verticais + trilho de espaços sem pin (§06) → trilho de "frames" (§07) → deck swipe de pilotos (§08) → texto calmo (§09) → case antes de logos (§10) → CTA final de silêncio (§11) → bottom sheet full-height (modal).
- **Viewports de QA:** 320, 375, 390, 430, 768, 1024, 1440, 1920 (+ 1280/1366/1536 para o limite exato da navbar).
- **Unidades:** `svh` em hero/runway (estável contra a barra de endereço), `dvh` em overlays; `env(safe-area-inset-*)` em header/barra fixa/sheet.
- **CTA fixo mobile:** barra inferior com "Crie um projeto com a Scuderia" + botão WhatsApp 52×52; aparece quando o CTA do hero sai do viewport, some no §11/footer/modal/menu/teclado aberto.
- **Trilhos horizontais:** sempre `scroll-snap` nativo com "peek" do próximo card, nunca pin/hijack horizontal no mobile (só a Caterham é pinada, e verticalmente).
- **Texto:** nunca `line-clamp` escondendo conteúdo; corpo ≥16px (evita zoom automático do iOS em inputs); `text-wrap: balance/pretty` nos títulos.
- **Dark-mode do SO:** a página é escura por definição (`color-scheme: dark` na maior parte); impedir inversão forçada do Chrome Android/Samsung Internet.
- **Lacunas de material específicas de mobile:** falta versão vertical (9:16) do POV Caterham dedicada (hoje só o `mobile.mp4` 40s genérico); as fotos de `estrutura/` (522px de largura) são fracas em DPR3 — pedir originais ≥1080px antes de produção final do trilho da dobra 06.

---

## 15. PROPOSTA VISUAL DE CADA DOBRA

Curva de ritmo (ordem da copy, sem reordenar — §13.2): IMPACTO(Hero) → CONTEXTO(Sobre) → AUTORIDADE(Faixa) → DESCOBERTA(Ecossistema) → IMERSÃO(Experiências) → **IMPACTO máximo(Caterham)** → RESPIRO/espaço(Bandeiras Empresarial) → DESCOBERTA rápida(Conteúdo) → IMPACTO/presença→bastidor(Pilotos & Time) → RESPIRO(Palestras) → PROVA(Marcas/PETRONAS) → CONVERSÃO(CTA final).

### NAVBAR
- **Papel:** utilitário, sempre presente. **Composição:** logo à esquerda (link para o topo) + 8 itens em 3 grupos visuais (ver §13.1) + CTA "Fale com o time" (WhatsApp) à direita. Transparente sobre o Hero, sólido (`--asfalto`, sem `backdrop-filter` pesado) após ~60px de rolagem.
- **Assets:** logo `Brand/1124_Bandeiras_RGB_L-RW.png` reexportado em SVG (hoje 8000×2392px, decodifica ~76 MB de RAM — correção obrigatória de performance).
- **Motion:** mudança de fundo por IO em sentinela (não scroll listener), FAST, `ease-brake`. Item ativo marcado por sublinhado + peso (nunca só cor).
- **Mobile:** barra compacta (56px) + botão "Menu" abrindo `<dialog>` de tela cheia com os 8 itens + WhatsApp no rodapé; header some ao rolar para baixo, volta ao rolar para cima (nunca enquanto há foco dentro dele).

### 01 · HERO
- **Silhueta:** H1 protagonista + fatia vertical de imagem/vídeo real sangrando para a borda. Sem splash, sem intro longa, sem carrossel.
- **Composição:** eyebrow "SCUDERIA BANDEIRAS" pequeno; H1 "Muito além das pistas." em Special Gothic gigante (`--t-mega`); os dois parágrafos da copy em Inter; CTA primário "Crie um projeto com a Scuderia" (abre WhatsApp até o formulário existir, depois abre modal com interesse "Ainda não sei") + link "Conheça o ecossistema" (rola até §03).
- **Assets reais a usar:** ordem de preferência — (1) still `Brand/Imagem/seção-2/uma-experiencia-criada-para-ser-vivida.jpg` (Caterham solitário em reta cinematográfica, 5000×3333) ou recorte vertical de `assets/videos/desk.mp4`/`mobile.mp4` (helicóptero + Caterham + POV) como vídeo de fundo com poster; (2) se o direito de uso desse material externo não for aprovado a tempo, `Imagens/final/imagem final@2x.png` (F3 na parede de tijolo do lobby) ou `Imagens/complexo/5.png` (fachada ao entardecer). **Nunca** `hero-conexoes-v2.png` (IA, speed lines) nem `imagem topo desk/mobile.png` (contém Rubens/Rafael + velocímetro + bandeira quadriculada).
- **Motion:** coreografia de entrada ≤800ms (imagem assenta 1.06→1, H1 sobe por palavra, parágrafos e CTA em sequência), tudo com `fill-mode:backwards` (nunca opacity:0 no LCP). Vídeo, se usado: `muted playsinline loop`, pausa fora da viewport, botão de pausa visível.
- **Mobile:** imagem/vídeo em 54svh no topo sangrando, H1 sobreposto com gradiente, parágrafos e CTA abaixo; CTA sempre visível sem rolar em ≥375×667.

### 02 · SOBRE
- **Silhueta:** faixa aérea larga + texto em dois níveis tipográficos. Não é split lateral, não tem cards.
- **Composição:** foto aérea do complexo (`Imagens/complexo/6.png`) em faixa larga no topo; abaixo, o 1º parágrafo da copy como lead grande, os parágrafos 2–4 em corpo, o 5º parágrafo como fechamento em display sentença. **Sem headline** (a copy aprovada não tem título nesta seção — ver §16, item 2): reservar um slot de título que só aparece se o usuário aprovar um; o layout funciona íntegro sem ele.
- **Assets:** `Imagens/complexo/6.png` (aérea) ou `complexo/5.png`. Risco factual ("competição", "corridas", "plataforma proprietária") tratado tipograficamente: esses trechos ficam em corpo comum, nunca em display grande (ver §16, item 1 — decisão factual, não de design).
- **Motion:** reveal por parágrafo (4 gatilhos IO separados, sem stagger entre eles — cada um entra quando é lido); 1 hairline como âncora de hierarquia, já que não há título.
- **Mobile:** aérea em 4:3 sangrando à direita, coluna única, ~25 linhas de leitura (medido por H, cabe sem "ler mais").

### FAIXA DE AUTORIDADE
- **Silhueta:** linha de créditos tipográfica — nunca card de equipe, nunca placar/tabela de posição.
- **Composição:** 4 colunas (Nelsinho, Christian, Ingo, Átila, na ordem da copy), nome em Special Gothic + credencial de uma linha em Inter; miniatura pequena opcional (recorte quadrado do retrato). Link "O time completo →" ao final, âncora para a dobra 08.
- **Assets:** `pilotos-premium/{nelson-piquet,cristian-fitipalidi,ingo-hoffman,atila-abreu}.jpg` (miniatura, se usada).
- **Motion:** hairline desenha (SLOW) + 4 nomes revelam em stagger curto (180ms total). Sem count-up nas credenciais (12 títulos, 3× Daytona) — contar até 12 trivializa e lembra placar, proibido.
- **Mobile:** 4 linhas empilhadas, mesmo tratamento.

### 03 · O ECOSSISTEMA
- **Silhueta:** título pendurado + mosaico de cinco peças reais (pilotos, carros, pista, estrutura, produção) + lista tipográfica dos 8 itens do ecossistema. **Único lugar da página** onde o princípio de recombinação (§1) ganha forma visual explícita — não repetido como widget nas dobras seguintes.
- **Composição:** H2 "O automobilismo é o ponto de partida." + H3 "Tudo se conecta a partir dele."; abaixo, cinco tiras verticais lado a lado (uma por peça), cada uma com legenda de uma palavra tirada da própria frase da copy ("Pilotos, carros, pista, estrutura e produção são as mesmas peças"); por fim, os 8 itens do ecossistema como lista de filetes, sem ícone, sem card.
- **Assets:** Pilotos → `pilotos-premium/atila-abreu.jpg`; Carros → `o que é/esporte.png` (oficina, recorte fechado no mecânico — evita reforçar "carro de campeonato"); Pista → material Caterham externo se aprovado (senão, recorte neutro sem afirmar Caterham); Estrutura → `complexo/3.png`; Produção → recorte do mosaico do time (após a correção do §6.3/§16 item 6).
- **Motion:** as cinco tiras entram com pequeno deslocamento (stagger 60ms, teto 5 itens), uma vez, ao entrar em viewport. Sem "docking" com highlight cruzado entre item↔peça (essa camada extra de interação, proposta por F, exigiria um mapeamento editorial novo não aprovado pela copy — cortada nesta direção, ver §16 item 13).
- **Mobile:** tiras viram carrossel horizontal nativo com `scroll-snap`; lista dos 8 itens em linhas de largura total.

### 04 · EXPERIÊNCIAS & PROJETOS (Team Building)
- **Silhueta:** panorama full-bleed + crescendo tipográfico dos 5 princípios + faixa clara com "Também no escopo".
- **Composição:** abre pelo objetivo do comprador (H2 "Sua empresa pode viver o automobilismo de dentro."); panorama de Team Building; H3 "Leve a experiência da pista para dentro do seu time."; as cinco frases em negrito da copy ("Trabalho em equipe.", "Confiança."...) como crescendo tipográfico (cada uma maior que a anterior, ou com foco de leitura por scroll); depois "Também no escopo" (Caterham para Empresas / Ativações de Marca / Experiências Corporativas / Projetos Personalizados) como faixa tipográfica quieta, sem cards; fecho com CTA `data-origin="team-building"`.
- **Assets:** `Imagens/race experience/RACE EXPERIENCE.png` (karts/drift — **não é Caterham**, usar apenas em contexto de "dinâmicas" genéricas do Team Building, nunca legendado como Caterham — correção do Crítico, P1-7); se aprovado, material externo do ensaio Capuava (`team-bulding/carrossel-animado/*`, `pit-stop-challenge.jpg`, `reaction-light.jpg`) para "eu consigo imaginar meu time ali".
- **Motion:** reading focus nas 5 frases (opacity .4→1 ao cruzar o centro da viewport); foto com clip reveal + assentar, 1× por viewport.
- **Mobile:** panorama com pan horizontal leve; crescendo em coluna única; "Também no escopo" em trilho snap de 4 cards.

### 05 · CATERHAM — REDESENHADA (a mudança mais importante desta direção)
**Esta dobra foi redesenhada do zero.** O relatório D (e F, ao especificar o motion) a desenhou 100% tipográfica por desconhecer o material real. O Crítico confirmou, abrindo e medindo os arquivos, que existe material profissional de POV de passageiro, vídeo 1080p do conjunto, vídeo 4K por modelo e still 5000×3333 — tudo fora da pasta do projeto, em `Projeto-caterham\Caterham - Festival - Interlagos\`. Esta dobra usa esse material como protagonista, condicionado à aprovação de direitos de imagem (§16, item 6).

- **Silhueta:** runway sticky de três momentos (POV → ficha de números → fecho vermelho), a única dobra da página com pin de narrativa tanto no desktop quanto no mobile.
- **Composição concreta (desktop, wrapper de ~2,4 viewports, palco `position:sticky`):**
  - **Beat A (0–40% do runway):** palco full-bleed com o vídeo/still POV do passageiro — `Brand/Imagem/seção-5/drive-experience.jpg` como still de abertura/poster, ou um recorte de 8–12s de `assets/videos/desk.mp4` (capacete do piloto em primeiro plano, mãos no volante, pista à frente, sem áudio no loop de fundo). H2 da copy ("Você no banco do passageiro. Um piloto profissional no volante.") entra por máscara de texto sobre gradiente inferior escuro.
  - **Beat B (40–75%):** troca para os vídeos/stills por modelo (`assets/videos/cars/ss600.mp4`/`420r.mp4`, ou stills de `assets/images/cars/gallery-01..05` — lateral, roda, escapamento, como detalhes de engenharia ao lado do texto) enquanto a ficha de números da copy aparece em tipografia tabular gigante: "84 cv para 490 kg." e "Ou 420R: 210 cv para 535 kg." — nunca com barra/velocímetro/HUD, só numeral + unidade. Contador (§12.4) permitido só aqui, 1× por par de números.
  - **Beat C (75–100%):** o palco despina; bloco full-bleed no vermelho real da marca (`--vermelho` #FF4747, texto em `--asfalto` para passar AA — ver §9.2) com "O piloto conduz." / "Você sente." em display enorme. É o único bloco de vermelho cheio de toda a página (P13 do benchmark C — o momento vermelho).
  - Depois do runway: fluxo normal com o restante do texto da copy (Race Experience, adaptação a eventos, Team Building) e CTA `data-origin="caterham"`.
- **Assets reais a usar (condicionados a §16 item 6):** `drive-experience.jpg`, `assets/videos/desk.mp4`/`mobile.mp4` (recortados a 8–12s, sem áudio, reencodados para ≤2,5MB desktop/≤1,2MB mobile), `ss600.mp4`/`420r.mp4` (versões web já em 1280×720, ~4,6MB — recomprimir para o orçamento de página), `assets/images/cars/gallery-01..05`. Fallback documentado (não plano principal): se os direitos de uso não forem aprovados a tempo do lançamento, a dobra cai para o tratamento 100% tipográfico original de D (ficha de números + fecho vermelho, sem mídia) — que continua funcionando sozinho, só com menos impacto.
- **Motion:** vídeo toca em tempo real (nunca escrubado pelo scroll — a copy diz que o visitante é passageiro, não piloto do scroll); o único elemento realmente escrubado é a transição de escala do quadro de vídeo no Beat C. Reduced-motion: sem pin, os 3 beats empilham estaticamente, vídeo mostra só poster + botão "Reproduzir".
- **Mobile:** runway de ~220svh, `position:sticky`, 3 beats por sentinelas IntersectionObserver (sem scroll listener); vídeo em faixa 16:9/9:16 no topo do palco (~42svh) com a cópia rolando por baixo — mesma estrutura do desktop, proporção diferente.

### 06 · BANDEIRAS EMPRESARIAL
- **Silhueta:** título em duas escalas (a headline protegida) + trilho horizontal de espaços reais ("a visita") + ficha de números + lista corrida dos usos.
- **Composição:** headline protegida, **texto intacto**, tratada em duas alturas tipográficas: "O automobilismo não é o tema do evento." menor, "É o endereço dele." em display gigante; trilho horizontal com `scroll-snap` (desktop: pinado linear, ~3–3,5 viewports — ver orçamento §12.3; mobile: caminhada vertical sem pin) passando por auditório, Espaço Motor, Arena Multiuso, coworking, heliponto, restaurante/área verde, terminando na oficina onde os carros são preparados; números da copy (300 pessoas, 40+ coworking, 350m², 310m²) como ficha de dados junto a cada cômodo; lista das seis frases de uso ("Convenções e reuniões de liderança.", "Lançamentos."...) como texto corrido, não cards; teaser do case PETRONAS com slot de vídeo (still até o vídeo existir).
- **Assets:** `Imagens/complexo/1,3,5,6,7.png`, `Imagens/local/auditorio.jpg` (reduzida — hoje 13,3MB), `Imagens/local/gastronomia.jpg`, `Imagens/o que é/estrutura.png` (sala com capacidade), `Imagens/estrutura/*` (originais em resolução melhor que 522px devem ser pedidos — pendência de produção). **Peso de imagem no trilho resolvido:** servir a 1280px de largura (130KB AVIF medido), não 1920px (245KB) — a largura retina estoura o orçamento de motion sem travar no pin, conforme a medição real do pipeline (conflito F×G resolvido a favor da largura menor).
- **Motion:** trilho horizontal linear (o scroll é o passo, sem aceleração — ecoa "quem caminha"), 1 camada de parallax leve por cômodo; legendas em reveal ao entrar no cômodo.
- **Mobile:** cenas verticais empilhadas (~70svh cada), sem pin/hijack, legenda `position:sticky` que troca em cascata.

### 07 · CONTEÚDO & BRANDED CONTENT
- **Silhueta:** contact sheet irregular + tabela tipo "call sheet" + linha do tempo tipográfica.
- **Composição:** grade de fotos em tamanhos variados, legendadas com as próprias palavras da frase da copy ("Carro em pista, box, briefing, oficina, auditório, estúdio de podcast."); as três ofertas (Branded Content / Campanhas & Gravações / Conteúdo com Pilotos) como linhas de tabela; fecho com "Do briefing na oficina ao pódio..." e CTA `data-origin="conteudo"`.
- **Assets:** `Imagens/o que é/esporte.png` (box/oficina), `Imagens/local/auditorio.jpg` (auditório), `Imagens/experiencia do evento/Truck Lounge.png` (Festival de Interlagos). **Faltam** frames reais de "briefing" e "estúdio de podcast" — pedido de produção, não inventar com imagem genérica.
- **Motion:** frames entram em stagger por coluna; linha do tempo com `scroll` linear acendendo os marcos citados na copy — sem desenhar pódio/troféu literalmente (reforçaria "campeonato").
- **Mobile:** contact strip horizontal com `scroll-snap`, linha do tempo vira barra de progresso do próprio strip.

### 08 · PILOTOS & TIME
- **Silhueta:** retrato que troca (presença) → mosaico de bastidor (o time). Duas metades com gramática de motion diferente (mais cinematográfico → mais denso/rápido, ecoando "operação").
- **Composição:** H2 "Quatro nomes do automobilismo, e um time inteiro por trás deles."; os 4 pilotos na ordem da copy, cada um com retrato + nome + papel + bio; depois "Muito além dos nomes conhecidos." com o mosaico do time e o parágrafo "mais de 50 profissionais"; fecho com seta apontando para Palestras.
- **Assets:** `pilotos-premium/{nelson-piquet,cristian-fitipalidi,ingo-hoffman,atila-abreu}.jpg`; `Imagens/nosso time/imagem time bandeiras desk.jpg` **somente após confirmação humana** de que Rubens Barrichello e Rafael Suzuki não estão nele (§16, item 6) — sem essa confirmação, esta dobra não pode ir ao ar com esse arquivo.
- **Motion:** retrato com clip reveal + assentar, nome/credencial com mask por palavra (parte 1); na parte 2 (bastidor), reveals mais rápidos e menores, sem clip, ecoando "menos cinema, mais oficina".
- **Mobile:** deck horizontal com `scroll-snap` para os 4 pilotos; mosaico do time fatiado em 3 recortes empilhados.

### 09 · PALESTRAS & CONTEÚDO COM PILOTOS
- **Silhueta:** coluna tipográfica estreita, muito espaço — a dobra mais calma da página (respiro antes da prova final).
- **Composição:** H2 + parágrafos de apoio; 4 linhas (nome + temas), na ordem da copy (Átila, Christian, Ingo, Nelsinho); nota de agenda em itálico tratada como texto legível (nunca "letra miúda escondida"); CTA `data-origin="palestra"`.
- **Assets:** miniaturas pequenas opcionais de `pilotos-premium/*`, sem imagem grande.
- **Motion:** único movimento — as 4 linhas entram como 1 grupo (stagger curto). Sem foto animada, sem parallax, sem hairline própria.
- **Mobile:** igual, já é quase estático por natureza.

### 10 · MARCAS & PARCEIROS (PETRONAS)
- **Silhueta:** case em destaque primeiro (prova > decoração), lista tipográfica de marcas depois.
- **Composição:** H2 "PETRONAS: da pista ao Bandeiras Empresarial."; slot de vídeo/still do case (vazio até o material chegar — nunca com texto de pendência visível) + os dois parágrafos da copy intactos; slot de depoimento nomeado (vazio até aprovação — ver §16, item 6, sobre o vídeo-depoimento já existente do Everton Silva/PETRONAS, condicionado a autorização por escrito); só depois, a lista de marcas em texto tipográfico (nunca a imagem raster `MARCAS PARCEIRAS.png`, cuja lista diverge da copy aprovada).
- **Assets:** `pilotos-premium/atila-abreu.jpg` como prova de trajetória; still de `Imagens/local/auditorio.jpg` **apenas se confirmado** que é de fato a convenção PETRONAS (não confirmado hoje — §16, item 5).
- **Motion:** imagem do case com clip reveal + assentar (1×); logos (quando existirem, aprovados) entram como um único grupo, sem stagger individual — decoração não merece coreografia própria.
- **Mobile:** case em coluna, lista de marcas em texto corrido.

### 11 · CTA FINAL
- **Silhueta:** quase vazia — bookend com o Hero, mesma família de composição em estado calmo.
- **Composição:** eyebrow "COMECE UM PROJETO"; H2 "Qual projeto sua empresa quer criar?"; frase de apoio; CTA primário "Crie um projeto com a Scuderia" + "Prefere conversar antes? Chame no WhatsApp"; nenhuma foto de carro em movimento, muito espaço vazio.
- **Assets:** opcionalmente `Imagens/final/imagem final@2x.png` em opacidade reduzida, ecoando o Hero sem repeti-lo pixel a pixel.
- **Motion:** headline + botão entram como 1 grupo, sem stagger. Botão: hover só de cor, `:active` `scale(.98)` — sem "magnetic", sem pulso.
- **Mobile:** barra CTA fixa some aqui (o CTA já é o foco da tela).

### MODAL / FORMULÁRIO
- **Comportamento:** ver §13.4/§13.5 — desligado (abre WhatsApp) até existir endpoint real; campos e pré-seleção conforme a origem do clique.
- **Composição:** `<dialog>` nativo, título "Vamos criar um projeto?"; campos na ordem da copy (Nome, Empresa, Telefone/WhatsApp, E-mail, interesse, quando/quantas — opcional, como prefere seguir, detalhar — opcional); consentimento; botão "Enviar para o time comercial".
- **Motion:** entrada `translateY+opacity` MEDIUM `ease-brake`; saída FAST `ease-trail` (nunca faz esperar para sair). Sem shake em erro.
- **Mobile:** sheet `100dvh`, cabeçalho fixo, botão de envio no fluxo (não fixed) enquanto o teclado está aberto.
- **Tela de sucesso:** "Recebemos." só aparece com confirmação 2xx real do servidor — nunca antes disso.

### FOOTER
- **Composição:** "Muito além das pistas." (retoma o Hero); os 9 links de navegação + Contato; endereço, WhatsApp, `scuderiabandeiras.com.br`; e-mail/CNPJ só aparecem quando os dados existirem (hoje pendentes — §16, item 12).
- **Assets:** logo em SVG (reexportado, ver §6.4).
- **Motion:** reveal simples do bloco inteiro, sem coreografia própria.

---

## 16. DECISÕES E APROVAÇÕES PENDENTES DO USUÁRIO

Lista consolidada, deduplicada e numerada — nada abaixo foi decidido por este documento. São as únicas coisas que faltam antes (ou durante) a implementação.

**Copy / fatos / jurídico**
1. **Risco factual:** a copy afirma "competição", "corridas", "um dos principais ecossistemas de automobilismo do Brasil", "plataforma proprietária" (§02/§03) enquanto `INSUMOS-FINAIS §18` diz que a Scuderia não disputa categoria oficial hoje. É decisão jurídica/de copy (ajuste de texto, nota legal, ou aceite formal do risco) — o design só evita amplificar (sem placar/grid/heritage), não resolve a causa.
2. **§02 Sobre sem headline:** confirmar se "O que Átila Abreu construiu não cabe em uma equipe." entra ou não (hoje ausente do arquivo aprovado).
3. **Localidade de Capuava:** a copy diz "Campinas"; o Guia do Participante do Race Experience (fonte externa) diz "Indaiatuba, SP" — confirmar antes de qualquer legenda de foto.
4. **Ordem de seções:** aprovar ou rejeitar a proposta (não aplicada nesta direção) de mover a Faixa de Autoridade para antes da §02 Sobre.
5. **Identificação de fotos:** confirmar se `Imagens/local/auditorio.jpg` é de fato a convenção PETRONAS; identificar qual foto é o "Espaço Motor"; identificar as pessoas nos murais do complexo (direito de imagem de terceiros, alguns possivelmente falecidos).

**Assets / direitos de imagem**
6. **Uso do material Caterham externo** (`Projeto-caterham\Caterham - Festival - Interlagos\`), base do redesenho da dobra 05: confirmar (a) que a Scuderia tem direito de uso desse material fora do projeto irmão; (b) autorização de imagem dos convidados/participantes reconhecíveis; (c) revisão dos patches/patrocinadores visíveis nos macacões antes de publicar; (d) aprovação por escrito da PETRONAS para o vídeo-depoimento nomeado (Everton Silva) usado na dobra 10.
7. **Confirmar Rubens Barrichello/Rafael Suzuki no mosaico "SOMOS TODOS SCUDERIA BANDEIRAS"** (`imagem time bandeiras desk.jpg`) — única prova visual de "mais de 50 profissionais" (dobra 08); sem essa confirmação humana, o arquivo não pode ir ao ar.
8. **Cor oficial de marca:** este documento adota `#FF4747` (medido no logo real) como base, com um derivado calculado `#C81E1E` para os dois usos em que o vermelho puro reprova AA (texto sobre papel claro; botão com texto branco). Se a marca exigir o vermelho puro em qualquer um desses dois usos, sem o derivado, essa combinação específica reprova AA e não tem correção sem mudar cor ou fundo — decisão pendente, não resolvida sozinha aqui (ver §9.3).
9. **Fonte oficial:** confirmar adoção de Special Gothic Condensed One (confirmada existente e gratuita no Google Fonts) em vez de Barlow Condensed (atual no site).
10. **Lista final de marcas/logos** (§10): a copy já marca como "depende de aprovação comercial/jurídica" — decisão explícita de quais marcas entram, em SVG individual (a imagem `MARCAS PARCEIRAS.png` tem ~30 marcas, a copy lista 15 — não coincidem).

**Endpoint / CRM / analytics / WhatsApp**
11. **Endpoint do formulário:** qual CRM/e-mail/webhook recebe os leads — hoje não existe nenhum real; até isso ser definido, todo CTA abre WhatsApp (§13.5).
12. **Ferramenta de analytics/tracking:** GA4, Meta Pixel, Vercel Analytics ou nenhuma — decide também se banner de cookies/LGPD é necessário. Também pendente: página de Política de Privacidade linkada no consentimento do formulário, e e-mail/CNPJ do controlador no footer.
13. **Número de WhatsApp oficial único:** `+55 15 99857-0854` (copy, usado em 9 lugares hoje) vs. `5515981282808` (footer/`maintenance-overlay.js` atuais) — usar o número da copy em todos os lugares, incluindo o overlay, antes do próximo commit dele.

**Microcopy proposta — aguarda aprovação em bloco (nada disso está na copy aprovada)**
14. Rótulo "Menu" e "Fechar" do menu/modal.
15. Estados de botão: "Enviando…", "Tentar de novo", "Enviar pelo WhatsApp" (fallback de falha de envio).
16. Texto pré-preenchido de WhatsApp por CTA (ex.: "Olá, vim pelo site da Scuderia Bandeiras. Tenho interesse em {formato}.").
17. Mensagens de erro de formulário (campo a campo).
18. Link "Ir para o conteúdo" (skip link).
19. Frase de sucesso do formulário sem o trecho de prazo pendente (ex.: "Nosso time entra em contato para entender o projeto." — remoção do colchete é edição de estado vazio, não reescrita, mas ainda pede confirmação).
20. Legendas de fotos inferidas onde a copy não nomeia o espaço/frame (trilho da dobra 06, contact sheet da dobra 07) — só devem existir se o usuário confirmar o fato por trás de cada legenda (ver item 5).
21. `aria-label` de acessibilidade que descrevam imagens/ícones sem texto visível equivalente na copy.

**Estrutura/formulário**
22. Campo "Como você prefere seguir?" obrigatório ou opcional (a copy não marca) — recomendação de E é não bloquear o envio, mas é decisão do usuário.
23. Seleção única ou múltipla no campo "O que você gostaria de criar com a Scuderia?".
24. Se o Instagram institucional entra no footer/navbar da nova home (a copy aprovada do footer não o lista, mas o site atual tem).
25. Destino de "O time completo →" confirmado como âncora para a dobra 08 (assumido nesta direção) — confirmar se é isso mesmo.

---

## 17. PRÓXIMOS PASSOS DE IMPLEMENTAÇÃO

Ordem sugerida — cada fase depende da anterior estar fechada; nenhuma pode "vazar" para produção sem a fase de QA.

1. **Rodada de aprovações (§16) primeiro.** Em especial: cor de marca (item 8), confirmação do mosaico do time (item 7), risco factual (item 1) e uso do material Caterham (item 6) — essas quatro bloqueiam decisões de CSS/imagem que vêm depois. Sem elas, qualquer trabalho de token/pipeline corre risco de refação.
2. **Pipeline de mídia** (antes de qualquer HTML/CSS final): otimizar as imagens confirmadas (AVIF/WebP, larguras 480/800/1280/1920, nomes com hash — §6.4), reexportar os logos em SVG, e — se aprovado — editar/recomprimir o material Caterham externo (recorte de 8–12s do POV, recompressão dos vídeos por modelo) para os orçamentos definidos em §12.
3. **Base HTML/CSS estática**: tokens (cor §9, tipografia §8, spacing §10, grid §7), esqueleto semântico das 15 dobras com `id`s estáveis, navbar/footer, formulário **desligado** (CTAs abrindo WhatsApp — §13.5) até o endpoint (item 11) ser aprovado.
4. **Camada de motion** (§12): reveals, sticky/scroll-driven dentro do orçamento único de pin (§12.3), sempre com o par reduced-motion completo desde o primeiro commit — não como "adicionar depois".
5. **QA multi-viewport** (320/375/390/430/768/1024/1440/1920, com 1280/1366/1536 extras para a navbar): overflow horizontal, contraste real renderizado (não só calculado), reduced-motion, sem-JS, placeholders desligados em todos os estados, Lighthouse/Playwright para LCP/CLS/INP.
6. **Ativação condicional do formulário**: só depois do endpoint (item 11) responder de verdade em ambiente de teste — nunca lançar com o padrão antigo de "sucesso sempre".
7. **Gate de lançamento**: reintroduzir/manter o overlay de manutenção com guarda por hostname local (não removê-lo do commit até o dia do lançamento deliberado), garantir `doc/`, `reports/`, `scripts/` fora do `.vercelignore`/deploy, e só então desligar o overlay em um commit nomeado e explícito.

---

**Confirmação de integridade deste documento:** nenhuma palavra de `doc/copy-final-aprovada.md` foi reescrita, adicionada ou reordenada como decisão definitiva neste arquivo. Toda menção a texto novo está marcada como "microcopy proposta — aguarda aprovação" na seção 16. Este documento representa uma única direção coerente — não um cardápio de alternativas — com as divergências entre os relatórios A–H/K resolvidas nas seções 9 (cor), 12.3 (orçamento de pin), 13.1 (breakpoint da navbar), 13.4 (select/chips), 13.2/13.3 (ordem e destino de CTAs) e 15.05 (Caterham).
